server:
  message: Hello Spring Boot

spring:
  profiles:
    active: docker, common
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

logging:
  file:
    path: logs
