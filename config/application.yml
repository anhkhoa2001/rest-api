server:
  port: 8087
  message: Hello Spring Boot

spring:
  profiles:
    active: local, common
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
  cache:
    type: memcached

logging:
  file:
    path: logs
